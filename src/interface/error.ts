/**
 * Represents a generic error message object.
 */
export type IGenericErrorMessage = {
  path: string | number; // The path or identifier associated with the error.
  message: string; // The error message providing information about the error.
};
